<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://use.typekit.net/nvm4puz.css">
  <link rel="stylesheet" href="/resources/css/app.css">
  <link rel="icon" href="/storage/nudel.png" type="image/png">
  <title>Bowlissimo | Login</title>
</head>
<body>

  <header class="header">
   @include('pages/partials/header')
   @include('pages/partials/navigation')
  </header>

  <main class="main">
    <h1 class="überschrift">Login</h1>

    @if (error != undefined)
      Fehler: {{ error }}
    @endif

      <form action="/login" method="post" enctype="application/x-www-form-urlencoded" class="login_form">
        {{csrfField()}}
        <label for="nutzername" class="login_überschrift">Nutzername:</label>
        <input type="text" id="nutzername" name="nutzername" required autofocus>
    
        <label for="passwort" class="login_überschrift">Passwort:</label>
        <input type="password" id="passwort" name="passwort" required>
    
        <button type="submit" class="einloggen_button" style="margin-bottom: 20px; margin-top: 30px;">Einloggen</button>
      </form>
      <div class="zurück_button_container" onclick="window.history.back();">
        <button class="zurück_button">
            <img class="zurück_button_pfeil" src="/storage/pfeil_gelb.png" alt="Ein gelber Pfeil, der nach links zeigt.">
            <span class="zurück_button_text">Zurück</span>
        </button>
      </div> 
      <p class="keinkonto_text">Noch kein Konto? <a href="/register" class="registrieren_button">Registrieren</a></p>
  </main>
</body>

<footer>
  @include('pages/partials/footer')
</footer>

</html>
