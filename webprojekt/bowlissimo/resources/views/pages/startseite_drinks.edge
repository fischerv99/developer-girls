<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <link rel="stylesheet" href="/css/app.css">
</head>

<body>

    <header class="header">
        @include('pages/partials/header')
        <nav>
            <ul>
                <li><a href="/startseite_pasta" class="aktiv">PASTA-BOWLS</a></li>
                <li><a href="/startseite_drinks" class="inaktiv">DRINKS</a></li>
                <li><a href="/startseite_beilagen" class="inaktiv">BEILAGEN</a></li>
                <li><a href="/login" class="inaktiv">Login</a></li>   <!--  Login Button hinzufügen -->
            </ul>
           <!--  Warenkorb Button hinzufügen -->
            <ul class="warenkorb-nav">
                <li><a href="/warenkorb" class="warenkorb-button">Warenkorb (0)</a></li> <!-- 0 ist die Platzhalteranzahl, die später dynamisch geändert werden kann -->
            </ul>
        </nav>        
    </header>

    <main>

        <div class="überschrift">
            <h1>DRINKS</h1>
        </div>
        <div class="smoothies">
            <h2 class="überschrift_auswahl">SMOOTHIES</h2>
            <div class="smoothie_auswahl">
                @each (sorte in smoothies)
                <div class="smoothie_sorten" data-id="{{ sorte.getränke_id }}">
                    <img src="{{ sorte.bild }}" alt="{{ sorte.getränke_id }}" longdesc="Ein Bild von {{ sorte.getränke_id }} in Herzform." width="200" height="200"> 
                    <div class="auswahl">
                        <button class="auswahl_button">
                            <span class="auswahl_button_text">Auswählen</span>
                        </button>
                        <h4 class= "produkt_id">{{ sorte.getränke_id }}</h4>
                        <button class="details" onclick='details/:kategorie/:id'>
                            <span class="details_text">Details</span>
                        </button>
                    </div>
                </div>
            @endeach
            </div>
        </div>

        <div class="erfrischungsgetränke">
            <h2 class="überschrift_auswahl">ERFRISCHUNGSGETRÄNKE</h2>
            <div class="erfrischungsgetränke_auswahl">
                @each (sorte in erfrischungsgetränke)
                <div class="erfrischungsgetränk_sorten" data-id="{{ sorte.getränke_id }}">
                    <img src="{{ sorte.bild }}" alt="{{ sorte.getränke_id }}" longdesc="Ein Bild von {{ sorte.getränke_id}} in Herzform." width="200" height="200">
                    <div class="auswahl">
                        <button class="auswahl_button">
                            <span class="auswahl_button_text">AUSWÄHLEN</span>
                        </button>
                        <h4 class="produkt_id">{{ sorte.getränk_id }}</h4>
                        <button class="details_button" onclick='details/:kategorie/:id'>
                            <span class="details_button_text">DETAILS</span>
                        </button>
                    </div>
                @endeach
                </div>
            </div>
        </div>

        <div class="alkoholfreie_cocktails">
            <h2 class="überschrift_auswahl">ALKOHOLFREIE COCKTAILS</h2>
            <div class="cocktails_auswahl">

                @each (sorte in alkoholfreie_getränke)
                <div class="cocktails_sorten" data-id="{{ sorte.getränke_id }}">
                    <img src="{{ sorte.bild }}" alt="{{ sorte.getränke_id }}" longdesc="Ein Bild von {{ sorte.getränke_id }} in Herzform." width="200" height="200">
                    <div class="auswahl">
                        <button class="auswahl_button">
                            <span class="auswahl_button_text">AUSWÄHLEN</span>
                        </button>
                        <h4 class="produkt_id">{{ sorte.getränke_id }}</h4>
                        <button class="details_button"onclick='details/:kategorie/:id'>
                            <span class="details_button_text">DETAILS</h4>
                        </button>
                    </div>
                </div> 
                @endeach
            </div>
        </div>

    </main>

    <footer>
        @include('pages/partials/footer')
    </footer>

</body>
</html>