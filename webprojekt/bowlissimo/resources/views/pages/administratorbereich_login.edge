<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://use.typekit.net/nvm4puz.css">
    <link rel="stylesheet" href="/resources/css/app.css">
    <link rel="icon" href="/storage/nudel.png" type="image/png">
    <title>Bowlissimo</title>
</head>
<body>
    <header class="header">
        @include('pages/partials/header')
        @include('pages/partials/navigation')
    </header>
    <main class="main">
        <div>
            <a href="/login" class="einloggen_button">Anmelden als Kunde</a>
        </div>

        <div class="login">
            <h1 class="überschrift">Anmelden als Administrator</h1>

            @if (error != undefined)
                Fehler: {{ error }}
            @endif

            <form action="/administratorbereich_login" method="POST" class="login_form"> <!-- Formular für die Anmeldung -->
                {{csrfField()}}
                <!-- Benutzername -->
                <label for="nutzername" class="login_überschrift">Nutzername:</label>
                <input type="text" id="nutzername" name="nutzername" required>

                <!-- Passwort -->
                <label for="passwort" class="login_überschrift">Passwort:</label>
                <input type="password" id="passwort" name="passwort" required>

                <!-- Login Button -->
                <button type="submit" class="einloggen_button" style="margin-bottom: 20px; margin-top: 30px;">Anmelden</button>
            </form>
            <div class="zurück_button_container" onclick="window.history.back();" style="margin-left: 190px; margin-bottom: 20px;">
                <button class="zurück_button">
                    <img class="zurück_button_pfeil" src="/storage/pfeil_gelb.png" alt="Ein gelber Pfeil, der nach links zeigt.">
                    <span class="zurück_button_text">Zurück</span>
                </button>
            </div> 
        </div>
    </main>

    <footer>
        @include('pages/partials/footer')
    </footer>
    
</body>
</html>
